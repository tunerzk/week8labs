# week8labs
 I successfully deployed a Google Cloud HTTP Load Balancer serving traffic from a Managed Instance Group (MIG) running in a private subnet. The MIG instances use a startup script to install Apache and generate a custom webpage. A health check monitors the backend, and Cloud NAT provides outbound internet access for package installation.
